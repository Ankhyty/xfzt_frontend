# 新番协作写稿平台 · 前后端接口交互文档

| 项 | 值 |
|---|---|
| 文档版本 | v2.2（2026-08-19） |
| 后端地址（开发） | `http://localhost:8080`（联调时替换为后端机器 IP / 域名） |
| 接口前缀 | `/api/v1` |
| 数据格式 | 请求/响应均为 `application/json` |
| 认证方式 | `Authorization: Bearer {token}`（登录或静默授权接口获取，有效期 24h） |
| 时间戳约定 | 请求中的截止时间用 **Unix 秒级时间戳**；响应中的时间为 ISO 8601 字符串 |

**统一响应包装**

```json
{ "code": 200, "message": "描述", "data": { /* 业务数据 */ } }
```

**统一错误码**

| code | 含义 | 常见触发 |
|---|---|---|
| 400 | 参数错误 | 缺必填字段、格式不合法（如季度名、番剧不在清单、断言解析失败） |
| 401 | 未认证 | 未带 token / token 过期 / 断言签名无效或重放 |
| 403 | 无权限 | 越权改他人卡片、非管理员、季度已截稿、第三方应用未配置或 IP 不在白名单 |
| 404 | 不存在 | 卡片/季度/版本不存在 |
| 409 | 冲突 | 用户名、季度名重复 |
| 429 | 限流 | 触发频率限制，见 `data.retry_after` |
| 500 | 服务端错误 | 联系后端排查 |

**测试约定**：本文所有"测试用例"均可直接复制执行（先执行 §1.2 登录或 §1.4.3 换取 token 替换 `$TOKEN`）。所有"样例结果"为真实环境实测输出。另有一键回归脚本 `tests/run_tests.sh` 覆盖本文全部接口。

---

## 目录

- [1. 认证模块](#1-认证模块)
  - [1.1 用户注册](#11-用户注册)
  - [1.2 用户登录](#12-用户登录)
  - [1.3 当前身份查询](#13-当前身份查询)
  - [1.4 第三方静默授权（子模块）](#14-第三方静默授权子模块)
    - [1.4.1 背景与角色映射](#141-背景与角色映射)
    - [1.4.2 合作站点服务端配置（白名单）](#142-合作站点服务端配置白名单)
    - [1.4.3 静默授权换取 Token](#143-静默授权换取-token)
    - [1.4.4 自动建号与身份映射规则](#144-自动建号与身份映射规则)
    - [1.4.5 断言 JWT 规范与生成示例](#145-断言-jwt-规范与生成示例)
- [2. 画廊模块](#2-画廊模块)
  - [2.1 画廊列表](#21-画廊列表)
- [3. 卡片模块](#3-卡片模块)
  - [3.1 查看卡片详情](#31-查看卡片详情)
  - [3.2 创建卡片](#32-创建卡片)
  - [3.3 申请上传凭证（预签名）](#33-申请上传凭证预签名)
  - [3.4 提交版本（commit）](#34-提交版本commit)
  - [3.5 版本回滚](#35-版本回滚)
  - [3.6 删除卡片](#36-删除卡片)
- [4. 季度模块](#4-季度模块)
  - [4.1 季度列表](#41-季度列表)
  - [4.2 新建季度](#42-新建季度)
  - [4.3 设置截止时间](#43-设置截止时间)
  - [4.4 更新番剧清单](#44-更新番剧清单)
  - [4.5 新建番剧名词条](#45-新建番剧名词条)
  - [4.6 季度导出](#46-季度导出)
  - [4.7 删除季度](#47-删除季度)
- [5. 附录](#5-附录)
  - [5.1 角色与权限速查](#51-角色与权限速查)
  - [5.2 频率限制汇总](#52-频率限制汇总)
  - [5.3 OBS 存储约定](#53-obs-存储约定)
  - [5.4 静默授权安全约定](#54-静默授权安全约定)
  - [5.5 前端 Token 过期与静默重试最佳实践](#55-前端-token-过期与静默重试最佳实践)

---

# 1. 认证模块

## 1.1 用户注册

| 项 | 值 |
|---|---|
| 方法 / 路径 | `POST /api/v1/auth/register` |
| 认证 | 无需 |
| 说明 | 注册新账号。默认角色为 `author`，管理员由后端在数据库中指定 |

**请求体**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| username | string | ✅ | 登录名，全局唯一 |
| password | string | ✅ | 明文密码（服务端 bcrypt 加密存储） |
| nickname | string | ❌ | 显示昵称 |

**测试用例**

```bash
curl -X POST http://localhost:8080/api/v1/auth/register \
  -H 'Content-Type: application/json' \
  -d '{"username":"writer_a","password":"secret123","nickname":"阿莉"}'
```

**样例结果（200）**

```json
{ "code": 200, "data": { "user_id": 3 }, "message": "注册成功" }
```

**样例结果（用户名已存在，409）**

```json
{ "code": 409, "message": "用户名已存在" }
```

---

## 1.2 用户登录

| 项 | 值 |
|---|---|
| 方法 / 路径 | `POST /api/v1/auth/login` |
| 认证 | 无需 |
| 说明 | 校验密码，签发 JWT。后续所有需认证接口在请求头携带 |

**请求体**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| username | string | ✅ | 登录名 |
| password | string | ✅ | 密码 |

**测试用例**

```bash
curl -X POST http://localhost:8080/api/v1/auth/login \
  -H 'Content-Type: application/json' \
  -d '{"username":"writer_a","password":"secret123"}'
```

**样例结果（200）**

```json
{
  "code": 200,
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJleHAiOjE3ODU3NTczMjMsInVzZXJfaWQiOjN9.KqwUtsE_5o0ty7_HD_6HtAwi_EzBx53ysmspAruHung",
    "user_info": {
      "user_id": 3,
      "username": "writer_a",
      "nickname": "阿莉",
      "role": "author"
    }
  },
  "message": "登录成功"
}
```

**样例结果（密码错误，401）**

```json
{ "code": 401, "message": "用户名或密码错误" }
```

---

## 1.3 当前身份查询

| 项 | 值 |
|---|---|
| 方法 / 路径 | `GET /api/v1/auth/me` |
| 认证 | 必须 |
| 限流 | 100 次/小时/IP（429） |
| 说明 | 返回当前 token 对应的用户信息；前端刷新登录态、获取当前权限角色使用 |

**测试用例**

```bash
curl http://localhost:8080/api/v1/auth/me \
  -H "Authorization: Bearer $TOKEN"
```

**样例结果（200）**

```json
{
  "code": 200,
  "data": {
    "user_info": {
      "user_id": 3,
      "username": "writer_a",
      "nickname": "阿莉",
      "role": "author"
    }
  },
  "message": "ok"
}
```

**样例结果（未认证，401）**

```json
{ "code": 401, "message": "未认证或 token 已过期" }
```

---

## 1.4 第三方静默授权（子模块）

### 1.4.1 背景与角色映射

平台支持定向接入指定的特定第三方站点（如合作论坛、主站），不对外提供自助注册 API。通过服务端预置白名单配置，实现第三方站点的**已登录用户**免注册、免登录页地获取平台作者身份与 Token：

- **角色约定**：第三方站点的所有用户一律映射为平台 **`author`（作者）** 角色；
- **用户组信息**：断言中的 `groups` 仅作为信息透传展示，不参与角色提权；
- **全流程无感**：首次访问自动生成无密码本地账户并绑定；再次访问复用同一账号下发 24h 平台 Token。

```
第三方用户已在指定站点登录

指定站点后端                             本平台后端（已预置该站点配置）
   |-- 1. 生成短时断言 JWT（2~5 分钟有效）->|
   |-- 2. POST /api/v1/auth/external/exchange --|
   |                                       | 3. 校验 iss 白名单、验签、校验 aud/exp/jti
   |                                       | 4. 按 (iss, sub) 查找或自动创建本地 author 用户
   |<-- 5. 200 { token, user_info } -------|
   |-- 6. 将 token 交给指定站点前端 -------->|

之后用户持 Authorization: Bearer {token} 直接调用写稿、卡片创建与管理接口
```

### 1.4.2 合作站点服务端配置（白名单）

平台**不提供任何外部注册 API**。所有受信任的合作站点由平台管理员在后端配置文件（如 `config.json` / `.env`）或数据库中手动配置：

| 配置字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| app_id | string | ✅ | 合作站点唯一标识，如 `site_forum`，对应断言中的 `iss` |
| name | string | ✅ | 合作站点显示名称，如“主站论坛” |
| app_secret | string | ✅ | 双方约定的共享密钥（HS256 算法），线下安全交付 |
| algorithm | string | ✅ | 签名算法，默认推荐 `HS256`（亦支持 `RS256` / `ES256`） |
| public_key | string | ❌ | 使用 RS256/ES256 时必填，合作站点的公钥（PEM 格式） |
| enabled | bool | ✅ | 是否启用该站点接入。设为 `false` 可一键熔断拦截 |
| ip_allowlist | string[] | ❌ | 允许发起 exchange 的服务器出口 IP 列表；空数组 `[]` 表示不限制 |

**配置示例（JSON 格式）：**

```json
{
  "external_apps": {
    "site_forum": {
      "name": "主站论坛",
      "algorithm": "HS256",
      "app_secret": "J8k2mX9pQ4wR7tY3vB6nC1zF5hA0sD2g",
      "enabled": true,
      "ip_allowlist": ["192.168.1.100", "203.0.113.10"]
    }
  }
}
```

### 1.4.3 静默授权换取 Token

| 项 | 值 |
|---|---|
| 方法 / 路径 | `POST /api/v1/auth/external/exchange` |
| 认证 | 无需登录，**用断言本身认证** |
| 限流 | 按 app 1000 次/小时 + 按外部用户 10 次/分钟（429） |
| 说明 | 校验第三方签名断言，映射/自动创建本地用户，签发平台 JWT（24h，与 `login` 一致） |

**请求体**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| assertion | string | ✅ | 第三方签发的 JWT 断言，规范见 §1.4.5 |

**测试用例**

```bash
curl -X POST http://localhost:8080/api/v1/auth/external/exchange \
  -H 'Content-Type: application/json' \
  -d "{\"assertion\":\"$ASSERTION\"}"
```

**样例结果（200）**

```json
{
  "code": 200,
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJleHAiOjE3ODU3NTczMjMsInVzZXJfaWQiOjl9.xxxx",
    "user_info": {
      "user_id": 9,
      "username": "site_forum_forum_user_1234",
      "nickname": "小明",
      "role": "author",
      "provider": "site_forum",
      "external_user_id": "forum_user_1234",
      "groups": ["member"]
    }
  },
  "message": "静默授权成功"
}
```

**响应字段（data）**

| 字段 | 类型 | 说明 |
|---|---|---|
| token | string | 平台 JWT，有效期 24h，调用现有接口时放 `Authorization: Bearer {token}` |
| user_info | object | 用户信息（`user_id`, `username`, `nickname`, `role`, `provider`, `external_user_id`, `groups`） |

**样例结果（401 / 403 / 429）**

```json
{ "code": 401, "message": "断言无效或已过期" }
```
```json
{ "code": 401, "message": "断言已使用过，请勿重放" }
```
```json
{ "code": 403, "message": "第三方应用未配置或未启用" }
```
```json
{ "code": 403, "message": "来源 IP 不在白名单内" }
```
```json
{
  "code": 429,
  "data": { "action": "external_exchange", "identity": "user:site_forum:forum_user_1234", "limit": 10, "retry_after": 58 },
  "message": "操作过于频繁：external_exchange 次数已达上限（10 次/分钟），请稍后再试"
}
```

### 1.4.4 自动建号与身份映射规则

1. **用户名**：固定规则 `{provider}_{external_user_id}`，例如 `site_forum_forum_user_1234`，全局唯一，避免与普通注册账号冲突；
2. **昵称**：取断言 `name`（未携带时回退为 `external_user_id`），保持与第三方站点一致；
3. **角色**：一律为 `author`；
4. **无密码与持久绑定**：数据库建立 `(provider, external_user_id)` 唯一索引，首次自动建号，二次访问复用同一 `user_id`。

### 1.4.5 断言 JWT 规范与生成示例

**Payload 规范：**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| iss | string | ✅ | 第三方应用 ID，必须等于平台配置的 `app_id` |
| sub | string | ✅ | 第三方站点内用户唯一 ID，对该用户永久不变 |
| aud | string | ✅ | 本平台 exchange 接口完整 URL |
| iat | int64 | ✅ | Unix 秒，签发时间 |
| exp | int64 | ✅ | Unix 秒，建议 2~5 分钟后过期 |
| jti | string | ✅ | 唯一断言 ID（UUID），用于防重放 |
| name | string | ❌ | 展示昵称，自动建号时写入 |
| groups | string[] | ❌ | 第三方用户组，透传展示 |

> **时钟容差说明**：平台后端在校验断言 `iat` 与 `exp` 时，建议允许 **±60 秒的时钟漂移（Clock Skew）**，以避免两端服务器微小时间差异导致误判过期。

**第三方生成断言示例（Python）：**

```python
import time
import uuid
import jwt

payload = {
    "iss": "site_forum",                       # 平台分配的 app_id
    "sub": "forum_user_1234",                  # 该用户在第三方站点的唯一 ID
    "aud": "http://localhost:8080/api/v1/auth/external/exchange",
    "iat": int(time.time()),
    "exp": int(time.time()) + 300,             # 5 分钟有效
    "jti": str(uuid.uuid4()),
    "name": "小明",
    "groups": ["member"],
}

assertion = jwt.encode(
    payload,
    "双方线下约定的共享密钥",                     # HS256；RS256 时改为第三方私钥
    algorithm="HS256",
    headers={"kid": "v1"},
)
```

---

# 2. 画廊模块

## 2.1 画廊列表

| 项 | 值 |
|---|---|
| 方法 / 路径 | `GET /api/v1/gallery/items` |
| 认证 | **可选**：游客可看；带 token 时返回能否编辑的 flag `editable` |
| 限流 | 100 次/小时/IP（429） |
| 缓存 | 响应带 `Cache-Control: public, max-age=30`，并返回 `ETag` 供前端做版本校对 |

**请求头（缓存校对）**

| 头部 | 必填 | 说明 |
|---|---|---|
| If-None-Match | ❌ | 前端携带本地缓存中的列表 ETag；如果没有缓存，则视为首次拉取，不带该头 |

**校对规则**

1. 后端以当前画廊列表快照生成该列表资源的 `ETag`，并在正常响应中返回 `ETag: "{gallery_etag}"`。
2. 前端再次请求画廊列表时，带上本地缓存中保存的列表 ETag，例如 `If-None-Match: "gallery-v2"`。
3. 如果请求中的 ETag 与后端当前列表快照一致，后端直接返回 `304 Not Modified`，不再返回列表数据。
4. 如果列表发生变化，后端正常返回 `200` 和最新画廊列表，同时更新响应里的 `ETag`。

**查询参数**

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| season | string | ❌ | 按季度筛选，如 `?season=2026-07` |

**响应字段（data.items[]，单项结构参照 3.1）**

| 字段 | 类型 | 说明 |
|---|---|---|
| card_id | string | 卡片 ID |
| anime_name | string | 番剧名（来自季度番单） |
| season_tag | string | 所属季度，格式 `YYYY-MM` |
| owner | object | `{user_id, username, nickname}` |
| editable | bool | 当前登录用户是否为创建者且在截止日期前（游客恒为 false，管理员恒为 true） |
| current_version | string | 当前生效版本号 |
| all_version | array | 所有可回滚的版本号：[v1, v2, ...] |
| updated_at | string | 最近更新时间 |
| content_assets | object | 当前版本文件资产，见下表 |

**content_assets 字段**

| 字段 | 类型 | 说明 |
|---|---|---|
| text_url | string | 文字部分完整下载地址 |
| text_sha256 | string | 文字部分哈希（用于下载后完整性校验） |
| images | array | 全量原图文件清单：`[{relative_path, url, sha256}]`，从未 commit 时为空数组 |
| images_thumb | array | 全量缩略图文件清单：`[{relative_path, url, sha256}]`，文件名为原图名 + `_thumb` 后缀，从未 commit 时为空数组 |

**测试用例**

```bash
# 首次拉取，响应会带 ETag
curl -i http://localhost:8080/api/v1/gallery/items

# 二次拉取，带上本地缓存的列表 ETag
curl -i 'http://localhost:8080/api/v1/gallery/items' \
  -H 'If-None-Match: "gallery-v2"'

# 登录用户 + 季度筛选
curl -i 'http://localhost:8080/api/v1/gallery/items?season=2026-07' \
  -H "Authorization: Bearer $TOKEN"
```

**样例结果（200，节选单卡片）**

```http
HTTP/1.1 200 OK
Cache-Control: public, max-age=30
ETag: "gallery-v2"
```

```json
{
  "code": 200,
  "data": {
    "total": 1,
    "items": [
      {
        "card_id": "card_1785671781_u1",
        "anime_name": "葬送的芙莉莲 第二季",
        "season_tag": "2026-07",
        "owner": { "user_id": 1, "username": "writer_a", "nickname": "阿莉" },
        "editable": true,
        "current_version": "v2",
        "all_version": ["v1", "v2"],
        "updated_at": "2026-08-02T20:30:11+08:00",
        "content_assets": {
          "text_url": "https://at-trump.obs.ap-southeast-3.myhuaweicloud.com/users/user_1/cards/card_1785671781_u1/versions/v2/text/article.json",
          "text_sha256": "e3b0c44298fc1c149afbf4c8996fb924",
          "images": [
            {
              "relative_path": "images/cover.webp",
              "url": "https://at-trump.obs.ap-southeast-3.myhuaweicloud.com/users/user_1/cards/card_1785671781_u1/versions/v2/images/cover.webp",
              "sha256": "4e0cbff15c98c0c8046814bc762332cb82d44b5d9502322deebd01e9f3321533"
            }
          ],
          "images_thumb": [
            {
              "relative_path": "images_thumb/cover_thumb.webp",
              "url": "https://at-trump.obs.ap-southeast-3.myhuaweicloud.com/users/user_1/cards/card_1785671781_u1/versions/v2/images_thumb/cover_thumb.webp",
              "sha256": "8f14e45fceea167a5a36dedd4bea2543"
            }
          ]
        }
      }
    ]
  }
}
```

**样例结果（304，命中缓存）**

```http
HTTP/1.1 304 Not Modified
Cache-Control: public, max-age=30
ETag: "gallery-v2"
```

**样例结果（限流，429）**

```json
{
  "code": 429,
  "data": { "action": "read", "identity": "ip:::1", "limit": 100, "retry_after": 3599 },
  "message": "操作过于频繁：read 次数已达上限（100 次/小时），请 60 分钟后再试"
}
```

---

# 3. 卡片模块

## 3.1 查看卡片详情

| 项 | 值 |
|---|---|
| 方法 / 路径 | `GET /api/v1/cards/:card_id` |
| 认证 | **可选**：游客可看；带 token 时返回能否编辑的 flag `editable` |
| 限流 | 100 次/小时/IP（429） |
| 缓存 | 响应带 `Cache-Control: public, max-age=30`，并返回 `ETag` 供前端做版本校对 |

> **数据结构说明**：详情接口统一通过 `data.items[0]` 提取卡片详情对象（结构与画廊单项一致），若卡片不存在则返回 404。

**请求头（缓存校对）**

| 头部 | 必填 | 说明 |
|---|---|---|
| If-None-Match | ❌ | 前端携带本地缓存中的当前版本号作为 ETag；如果没有缓存，则视为首次拉取，不带该头 |

**校对规则**

1. 后端以卡片当前 `current_version` 作为该详情资源的版本标识，并在正常响应中返回 `ETag: "{current_version}"`。
2. 前端再次请求卡片详情时，带上本地缓存中保存的当前版本号，例如 `If-None-Match: "v2"`。
3. 如果请求中的 ETag 与后端当前生效版本一致，后端直接返回 `304 Not Modified`，不再返回正文数据。
4. 如果版本不一致，后端正常返回 `200` 和最新卡片详情，同时更新响应里的 `ETag`。

**查询参数**

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| version | string | ❌ | 按版本查看历史详情，如 `?version=v1`；不传则默认返回当前生效版本 |

**响应字段（data.items[]）**

| 字段 | 类型 | 说明 |
|---|---|---|
| card_id | string | 卡片 ID |
| anime_name | string | 番剧名（来自季度番单） |
| season_tag | string | 所属季度，格式 `YYYY-MM` |
| owner | object | `{user_id, username, nickname}` |
| editable | bool | 当前登录用户是否为创建者且在截止日期前（游客恒为 false，管理员恒为 true） |
| current_version | string | 当前生效版本号 |
| all_version | string[] | 所有可回滚的版本号：[v1, v2, ...] |
| updated_at | string | 最近更新时间 |
| content_assets | object | 当前版本文件资产，见下表 |

**content_assets 字段**

| 字段 | 类型 | 说明 |
|---|---|---|
| text_url | string | 文字部分完整下载地址 |
| text_sha256 | string | 文字哈希（用于下载后完整性校验） |
| images | array | 全量图片文件清单：`[{relative_path, url, sha256}]`，从未 commit 时为空数组 |
| images_thumb | array | 缩略图片文件清单：`[{relative_path, url, sha256}]`，文件名为原图名 + `_thumb` 后缀，从未 commit 时为空数组 |

**测试用例**

```bash
# 游客访问
curl http://localhost:8080/api/v1/cards/card_1785671781_u1

# 登录用户
curl 'http://localhost:8080/api/v1/cards/card_1785671781_u1?version=v1' \
  -H "Authorization: Bearer $TOKEN"
```

**样例结果（200）**

```http
HTTP/1.1 200 OK
Cache-Control: public, max-age=30
ETag: "v2"
```

```json
{
  "code": 200,
  "data": {
    "total": 1,
    "items": [
      {
        "card_id": "card_1785671781_u1",
        "anime_name": "葬送的芙莉莲 第二季",
        "season_tag": "2026-07",
        "owner": { "user_id": 1, "username": "writer_a", "nickname": "阿莉" },
        "editable": true,
        "current_version": "v2",
        "updated_at": "2026-08-02T20:30:11+08:00",
        "content_assets": {
          "text_url": "https://at-trump.obs.ap-southeast-3.myhuaweicloud.com/users/user_1/cards/card_1785671781_u1/versions/v2/text/article.json",
          "text_sha256": "e3b0c44298fc1c149afbf4c8996fb924",
          "images": [
            {
              "relative_path": "images/cover.webp",
              "url": "https://at-trump.obs.ap-southeast-3.myhuaweicloud.com/users/user_1/cards/card_1785671781_u1/versions/v2/images/cover.webp",
              "sha256": "4e0cbff15c98c0c8046814bc762332cb82d44b5d9502322deebd01e9f3321533"
            }
          ],
          "images_thumb": [
            {
              "relative_path": "images_thumb/cover_thumb.webp",
              "url": "https://at-trump.obs.ap-southeast-3.myhuaweicloud.com/users/user_1/cards/card_1785671781_u1/versions/v2/images_thumb/cover_thumb.webp",
              "sha256": "8f14e45fceea167a5a36dedd4bea2543"
            }
          ]
        }
      }
    ]
  }
}
```

**样例结果（304，命中缓存）**

```http
HTTP/1.1 304 Not Modified
Cache-Control: public, max-age=30
ETag: "v2"
```

**样例结果（限流，429）**

```json
{
  "code": 429,
  "data": { "action": "read", "identity": "ip:::1", "limit": 100, "retry_after": 3599 },
  "message": "操作过于频繁：read 次数已达上限（100 次/小时），请 60 分钟后再试"
}
```

---

## 3.2 创建卡片

| 项 | 值 |
|---|---|
| 方法 / 路径 | `POST /api/v1/cards` |
| 认证 | 必须 |
| 说明 | 创建空卡片档案（初始版本 v1），正文/图片经 §3.3 流程上传 |

**请求体**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| anime_name | string | ✅ | 番剧名，**必须在所属季度的 animes 清单中** |
| season_tag | string | ✅ | 季度名，**必须是已存在的季度** |

**测试用例**

```bash
curl -X POST http://localhost:8080/api/v1/cards \
  -H "Authorization: Bearer $TOKEN" -H 'Content-Type: application/json' \
  -d '{"anime_name":"胆大党 第二季","season_tag":"2026-07"}'
```

**样例结果（200）**

```json
{
  "code": 200,
  "data": {
    "card_id": "card_1785673381_u1",
    "anime_name": "胆大党 第二季",
    "season_tag": "2026-07",
    "current_version": "v1",
    "created_at": "2026-08-02T21:10:48+08:00"
  },
  "message": "创建成功"
}
```

**样例结果（400）**

```json
{ "code": 400, "message": "番剧名不在该季度候选清单中，请从 animes 数组中选择" }
```
```json
{ "code": 400, "message": "季度不存在，请从 GET /seasons 返回的列表中选择" }
```

---

## 3.3 申请上传凭证（预签名）

| 项 | 值 |
|---|---|
| 方法 / 路径 | `POST /api/v1/storage/presign-upload` |
| 认证 | 必须，且为卡片 Owner 或管理员 |
| 限流 | 30 次/小时（对公组共享），429 |
| 说明 | 为每个文件签发 OBS 直传通行证（PUT，5 分钟有效） |

**请求体**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| card_id | string | ✅ | 卡片 ID |
| next_version | string | ✅ | 即将上传的版本号，如 `v3` |
| files | array | ✅ | 文件数组，见下 |
| files[].relative_path | string | ✅ | 版本内相对路径，如 `text/article.json`、`images/hero.webp`、`images_thumb/hero_thumb.webp` |
| files[].content_type | string | 建议 | MIME 类型。正文建议使用 `application/json`，图片建议使用 `image/webp`。**直传时必须带一模一样的 Content-Type 头** |
| files[].sha256 | string | ❌ | 文件哈希（commit 时上报用） |

**文件组织约定**

1. `images/` 存原图，`images_thumb/` 存缩略图。
2. 缩略图文件名在原图基础上加 `_thumb` 后缀，例如 `images/hero.webp` 对应 `images_thumb/hero_thumb.webp`。
3. `files` 数组里原图和缩略图都要单独列出，后端会分别返回对应的预签名 URL。

**测试用例**

```bash
curl -X POST http://localhost:8080/api/v1/storage/presign-upload \
  -H "Authorization: Bearer $TOKEN" -H 'Content-Type: application/json' \
  -d '{
    "card_id": "card_1785671781_u1",
    "next_version": "v3",
    "files": [
        {"relative_path":"text/article.json","content_type":"application/json","sha256":"e3b0c442..."},
        {"relative_path":"images/scene1.webp","content_type":"image/webp","sha256":"9e107d9d..."},
        {"relative_path":"images_thumb/scene1_thumb.webp","content_type":"image/webp","sha256":"e4d909c2..."}
    ]
  }'
```

**样例结果（200）**

```json
{
  "code": 200,
  "data": {
    "card_id": "card_1785671781_u1",
    "version": "v3",
    "upload_tickets": [
      {
        "relative_path": "text/article.json",
        "upload_url": "https://at-trump.obs.ap-southeast-3.myhuaweicloud.com:443/users/user_1/cards/card_1785671781_u1/versions/v3/text/article.json?AccessKeyId=HPUAD...&Expires=1785674186&Signature=jJwTor...",
        "http_method": "PUT",
        "headers": { "Content-Type": "application/json" }
      },
      {
        "relative_path": "images/scene1.webp",
        "upload_url": "https://at-trump.obs.ap-southeast-3.myhuaweicloud.com:443/users/user_1/cards/card_1785671781_u1/versions/v3/images/scene1.webp?AccessKeyId=...&Signature=...",
        "http_method": "PUT",
        "headers": { "Content-Type": "image/webp" }
      },
      {
        "relative_path": "images_thumb/scene1_thumb.webp",
        "upload_url": "https://at-trump.obs.ap-southeast-3.myhuaweicloud.com:443/users/user_1/cards/card_1785671781_u1/versions/v3/images_thumb/scene1_thumb.webp?AccessKeyId=...&Signature=...",
        "http_method": "PUT",
        "headers": { "Content-Type": "image/webp" }
      }
    ]
  },
  "message": "预签名生成成功"
}
```

**样例结果（越权，403）**

```json
{ "code": 403, "message": "权限拒绝：您无权修改他人创建的卡片" }
```

**直传用法（客户端拿到 ticket 后，不经过后端）**

```bash
curl -X PUT --data-binary @article.json \
  -H 'Content-Type: application/json' \
  -H 'Cache-Control: public, max-age=31536000, immutable' \
  "{upload_url}"
```

---

## 3.4 提交版本（commit）

| 项 | 值 |
|---|---|
| 方法 / 路径 | `POST /api/v1/cards/:card_id/commit` |
| 认证 | 必须，Owner 或管理员 |
| 限流 | 10 次/小时（对公组共享），429 |
| 说明 | **全部文件直传成功后**调用：登记文件清单、切换版本指针 |

**请求体**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| version | string | ✅ | 与 presign 的 next_version 一致 |
| commit_message | string | ❌ | 提交说明 |
| files | array | 强烈建议 | 实际上传的文件清单 `[{relative_path, content_type, sha256}]`，画廊 content_assets 的数据源 |

**测试用例**

```bash
curl -X POST http://localhost:8080/api/v1/cards/card_1785671781_u1/commit \
  -H "Authorization: Bearer $TOKEN" -H 'Content-Type: application/json' \
  -d '{
    "version": "v3",
    "commit_message": "完成初稿",
    "files": [
      {"relative_path":"text/article.json","content_type":"application/json","sha256":"e3b0c442..."},
      {"relative_path":"images/scene1.webp","content_type":"image/webp","sha256":"9e107d9d..."},
      {"relative_path":"images_thumb/scene1_thumb.webp","content_type":"image/webp","sha256":"e4d909c2..."}
    ]
  }'
```

**样例结果（200）**

```json
{ "code": 200, "data": { "current_version": "v3" }, "message": "提交成功" }
```

**样例结果（403 / 429）**

```json
{ "code": 403, "message": "该季度已截稿，无法继续提交" }
```
```json
{
  "code": 429,
  "data": { "action": "submit", "identity": "user:1", "limit": 10, "retry_after": 3599 },
  "message": "操作过于频繁：submit 次数已达上限（10 次/小时），请 60 分钟后再试"
}
```

---

## 3.5 版本回滚

| 项 | 值 |
|---|---|
| 方法 / 路径 | `POST /api/v1/cards/:card_id/rollback` |
| 认证 | 必须，Owner 或管理员 |
| 说明 | 把版本指针切回历史版本，立即生效；OBS 文件不动 |

**请求体**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| target_version | string | ✅ | 目标版本，**必须曾 commit 过** |

**测试用例**

```bash
curl -X POST http://localhost:8080/api/v1/cards/card_1785671781_u1/rollback \
  -H "Authorization: Bearer $TOKEN" -H 'Content-Type: application/json' \
  -d '{"target_version":"v1"}'
```

**样例结果（200 / 404）**

```json
{ "code": 200, "data": { "current_version": "v1" }, "message": "回滚成功" }
```
```json
{ "code": 404, "message": "目标版本不存在" }
```

---

## 3.6 删除卡片

| 项 | 值 |
|---|---|
| 方法 / 路径 | `DELETE /api/v1/cards/:card_id` |
| 认证 | 必须，Owner 或管理员 |
| 说明 | 物理删除指定的卡片档案及其所有关联历史版本 |

**测试用例**

```bash
curl -X DELETE http://localhost:8080/api/v1/cards/card_1785671781_u1 \
  -H "Authorization: Bearer $TOKEN"
```

**样例结果（200）**

```json
{
  "code": 200,
  "data": { "card_id": "card_1785671781_u1" },
  "message": "卡片删除成功"
}
```

**样例结果（403 / 404）**

```json
{ "code": 403, "message": "权限拒绝：您无权删除他人创建的卡片" }
```
```json
{ "code": 404, "message": "卡片不存在" }
```

---

# 4. 季度模块

## 4.1 季度列表

| 项 | 值 |
|---|---|
| 方法 / 路径 | `GET /api/v1/seasons` |
| 认证 | 无需（游客可看） |
| 说明 | 前端筛选下拉、建卡番单下拉的**唯一数据源** |

**测试用例**

```bash
curl http://localhost:8080/api/v1/seasons
```

**样例结果（200）**

```json
{
  "code": 200,
  "data": {
    "total": 1,
    "items": [
      {
        "season_id": 2,
        "name": "2026-07",
        "deadline": null,
        "animes": ["葬送的芙莉莲 第二季", "死神 千年血战篇", "胆大党 第二季"],
        "created_at": "2026-08-02T21:10:48+08:00"
      }
    ]
  }
}
```

---

## 4.2 新建季度

| 项 | 值 |
|---|---|
| 方法 / 路径 | `POST /api/v1/seasons` |
| 认证 | **管理员** |

**请求体**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| name | string | ✅ | `YYYY-MM`，月 ∈ 01/04/07/10 |
| deadline | int64 | ❌ | Unix 秒时间戳 |
| animes | string[] | ❌ | 番剧候选清单，默认 `[]` |

**测试用例**

```bash
curl -X POST http://localhost:8080/api/v1/seasons \
  -H "Authorization: Bearer $ADMIN_TOKEN" -H 'Content-Type: application/json' \
  -d '{"name":"2026-07","animes":["葬送的芙莉莲 第二季","胆大党 第二季"]}'
```

**样例结果（200 / 400 / 403 / 409）**

```json
{ "code": 200, "message": "创建成功", "data": { "season_id": 2, "name": "2026-07", ... } }
```
```json
{ "code": 400, "message": "季度格式错误：应为 YYYY-MM，且月份只能是 01/04/07/10（如 2026-07）" }
```
```json
{ "code": 403, "message": "需要管理员权限" }
```
```json
{ "code": 409, "message": "该季度已存在" }
```

---

## 4.3 设置截止时间

| 项 | 值 |
|---|---|
| 方法 / 路径 | `PUT /api/v1/seasons/:season_id/deadline` |
| 认证 | **管理员** |

**测试用例**

```bash
curl -X PUT http://localhost:8080/api/v1/seasons/2/deadline \
  -H "Authorization: Bearer $ADMIN_TOKEN" -H 'Content-Type: application/json' \
  -d '{"deadline":1793577540}'
```

**样例结果（200）**

```json
{ "code": 200, "message": "截止时间已更新" }
```

---

## 4.4 更新番剧清单

| 项 | 值 |
|---|---|
| 方法 / 路径 | `PUT /api/v1/seasons/:season_id/animes` |
| 认证 | **管理员** |
| 说明 | 整体替换该季度的番单 |

**测试用例**

```bash
curl -X PUT http://localhost:8080/api/v1/seasons/2/animes \
  -H "Authorization: Bearer $ADMIN_TOKEN" -H 'Content-Type: application/json' \
  -d '{"animes":["葬送的芙莉莲 第二季","新追加的番"]}'
```

**样例结果（200）**

```json
{ "code": 200, "data": { "animes": ["葬送的芙莉莲 第二季", "新追加的番"] }, "message": "番剧清单已更新" }
```

---

## 4.5 新建番剧名词条

| 项 | 值 |
|---|---|
| 方法 / 路径 | `POST /api/v1/seasons/:season_id/animes` |
| 认证 | **作者** 或 **管理员** |
| 说明 | 给指定季度新增一条番剧名词条；若该番剧已存在则返回冲突 |

**请求体**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| anime_name | string | ✅ | 要新增的番剧名，不能为空 |

**测试用例**

```bash
curl -X POST http://localhost:8080/api/v1/seasons/2/animes \
  -H "Authorization: Bearer $TOKEN" -H 'Content-Type: application/json' \
  -d '{"anime_name":"败犬女主太多了！第二季"}'
```

**样例结果（200 / 400 / 403 / 409）**

```json
{
  "code": 200,
  "data": {
    "season_id": 2,
    "name": "2026-07",
    "animes": ["葬送的芙莉莲 第二季", "死神 千年血战篇", "胆大党 第二季", "败犬女主太多了！第二季"]
  },
  "message": "番剧名词条已新增"
}
```
```json
{ "code": 400, "message": "番剧名不能为空" }
```
```json
{ "code": 403, "message": "需要作者或管理员权限" }
```
```json
{ "code": 409, "message": "该番剧名已存在，请勿重复添加" }
```

---

## 4.6 季度导出

| 项 | 值 |
|---|---|
| 方法 / 路径 | `GET /api/v1/seasons/:season_id/export` |
| 认证 | **管理员** |
| 说明 | 返回该季度全部卡片**当前版本**的文件下载清单。后端不打包，客户端自行下载组 zip |

**测试用例**

```bash
curl http://localhost:8080/api/v1/seasons/2/export \
  -H "Authorization: Bearer $ADMIN_TOKEN"
```

**样例结果（200）**

```json
{
  "code": 200,
  "data": {
    "season": "2026-07",
    "total_cards": 2,
    "bucket_base": "https://at-trump.obs.ap-southeast-3.myhuaweicloud.com",
    "cards": [
      {
        "card_id": "card_1785671781_u1",
        "owner": { "user_id": 1, "username": "writer_a", "nickname": "阿莉" },
        "current_version": "v2",
        "files": [
          { "relative_path": "text/article.json", "url": "https://...", "sha256": "e3b0c442..." }
        ]
      }
    ],
    "generated_at": "2026-08-02T21:30:00+08:00"
  }
}
```

---

## 4.7 删除季度

| 项 | 值 |
|---|---|
| 方法 / 路径 | `DELETE /api/v1/seasons/:season_id` |
| 认证 | **管理员** |
| 说明 | 物理删除指定的季度档案，同时清理该季度下的所有候选番剧与已发布的评测卡片 |

**测试用例**

```bash
curl -X DELETE http://localhost:8080/api/v1/seasons/2 \
  -H "Authorization: Bearer $ADMIN_TOKEN"
```

**样例结果（200 / 403 / 404）**

```json
{ "code": 200, "data": { "season_id": 2 }, "message": "季度删除成功" }
```
```json
{ "code": 403, "message": "需要管理员权限" }
```
```json
{ "code": 404, "message": "季度不存在" }
```

---

# 5. 附录

## 5.1 角色与权限速查

| 操作 | 游客 | 普通注册作者 | 第三方静默授权作者 | 管理员 |
|---|---|---|---|---|
| 看画廊/卡片/季度列表 | ✅ | ✅ | ✅ | ✅ |
| 注册/登录/静默换票 | ✅ | ✅ | ✅ | ✅ |
| 查看当前身份（`/auth/me`） | ❌ | ✅ | ✅ | ✅ |
| 创建卡片 | ❌ | ✅ | ✅ | ✅ |
| 新建单个番剧名 | ❌ | ✅ | ✅ | ✅ |
| 上传/commit/回滚自己的卡片 | ❌ | ✅ | ✅ | ✅ |
| 修改他人卡片 | ❌ | ❌ 403 | ❌ 403 | ✅ |
| 删除自己的卡片 | ❌ | ✅ | ✅ | ✅ |
| 删除他人卡片 | ❌ | ❌ 403 | ❌ 403 | ✅ |
| 建季度/设截止/改番单/导出 | ❌ | ❌ 403 | ❌ 403 | ✅ |
| 删除季度 | ❌ | ❌ 403 | ❌ 403 | ✅ |
| 截稿后提交 | — | ❌ 403 | ❌ 403 | ✅ 豁免 |

## 5.2 频率限制汇总

| 动作 | 涉及接口 | 额度 | 计数主体 |
|---|---|---|---|
| read | `GET /gallery/items`、`GET /cards/:id`、`GET /auth/me` | 100 次/小时 | 访客 IP |
| modify | `POST /storage/presign-upload` | 30 次/小时 | 个人 / 对公组共享 |
| submit | `POST /cards/:id/commit` | 10 次/小时 | 个人 / 对公组共享 |
| external_exchange | `POST /auth/external/exchange` | 1000 次/小时 | 第三方应用（app） |
| external_exchange | `POST /auth/external/exchange` | 10 次/分钟 | 单个外部用户（provider + external_user_id） |

429 响应含 `data.retry_after`（冷却剩余秒数）。注意：限流计数发生在业务校验**之前**，被拒请求同样消耗额度。

## 5.3 OBS 存储约定

```
桶：at-trump（ap-southeast-3，公共读）
users/user_{用户ID}/cards/{卡片ID}/versions/{版本号}/
├── text/article.json      文字内容（固定名，内容为包含一句话简评、评分和正文三个部分）
├── images/
│   └── *.webp           原图（版本内唯一命名）
└── images_thumb/
    └── *_thumb.webp     缩略图（文件名为原图名 + `_thumb` 后缀）
```

- 缩略图文件统一放在 `images_thumb/` 下，文件名为原图名 + `_thumb` 后缀，例如 `images/hero_scene.webp` 对应 `images_thumb/hero_scene_thumb.webp`
- 如果前端需要同时展示原图和缩略图，上传时两者都应纳入同一个版本的 `files` 清单，并在 commit 时一并上报
- 直传 PUT 时带 `Cache-Control: public, max-age=31536000, immutable`（不参与签名，直接加）
- 客户端对文件 URL 做本地缓存；上传前算 SHA256，下载后用清单哈希校验
- 文件不可变（改内容 = 新版本新 URL），可放心永久缓存

## 5.4 静默授权安全约定

| 项 | 要求 |
|---|---|
| 断言有效期 | 2~5 分钟，越短越好；平台签发的 token 仍为 24h |
| 时钟容差 | 服务端验签断言时应允许 **±60 秒的时钟漂移**（Clock Skew），避免两端服务器时间差导致误拒 |
| 防重放 | `jti` 唯一 + 缓存至断言过期；同一 jti 二次使用直接拒绝 |
| audience | `aud` 必须等于平台配置的 exchange 接口完整 URL，防止断言被用于其他服务 |
| 算法白名单 | 只允许 `HS256 / RS256 / ES256`，拒绝 `none` 与弱算法 |
| 白名单管控 | 后端纯静态/数据库配置管理，不对外暴露任何注册接口 |
| IP 白名单 | 可选；开启后仅允许第三方服务器出口 IP 调用 exchange |
| 传输安全 | 全程 HTTPS；断言与 token 不落日志 |
| 审计 | 记录 provider、external_user_id、来源 IP、结果；失败不记敏感字段 |
| 密钥保管 | `app_secret` 由双方管理员线下交付，只存在于双方后端，**严禁下发到浏览器** |

## 5.5 前端 Token 过期与静默重试最佳实践

在第三方站点嵌入/调用本平台时，前端可以通过全局 HTTP 响应拦截器（如 Axios Interceptor）实现完全无感知的 Token 自动续期与错误重试：

1. **缓存 Token**：静默换票成功后，将平台 Token 存储在 `localStorage` / `sessionStorage` 中；
2. **401 拦截**：当接口请求返回 `401 未认证或 token 已过期` 时：
   - 暂停当前请求队列；
   - 触发第三方后端重新生成短时断言并调用 `POST /api/v1/auth/external/exchange` 换取新 Token；
   - 更新本地 Token 缓存；
   - 使用新 Token 自动重放之前失败的业务请求；
3. **避免死循环**：单次请求最多重试 1 次，若二次换票仍失败则向用户抛出重新登录第三方的提示。
