# 新番协作写稿平台 · 第三方静默授权接口文档

| 项 | 值 |
|---|---|
| 文档版本 | v1.1（2026-08-19） |
| 后端地址（开发） | `http://localhost:8080`（联调时替换为后端机器 IP / 域名） |
| 接口前缀 | `/api/v1` |
| 数据格式 | 请求/响应均为 `application/json` |
| 认证方式 | `Authorization: Bearer {token}`（静默授权接口换取，有效期 24h） |
| 接入机制 | **特定站点白名单 + 服务端静态配置**（平台后端手动录入合作方配置，不对外暴露注册接口） |

**统一响应包装**

```json
{ "code": 200, "message": "描述", "data": { /* 业务数据 */ } }
```

**统一错误码**

| code | 含义 | 常见触发 |
|---|---|---|
| 400 | 参数错误 | 缺少 `assertion`、断言 JSON 解析失败 |
| 401 | 未认证 | 断言签名无效 / 过期 / aud 不匹配 / jti 重放；token 缺失或过期 |
| 403 | 无权限 | 第三方应用未配置/未启用、IP 不在白名单 |
| 429 | 限流 | 触发频率限制，见 `data.retry_after` |
| 500 | 服务端错误 | 联系后端排查 |

**测试约定**：本文所有"测试用例"均可直接复制执行（先执行 §3.1 换取 token 替换 `$TOKEN`；`$ASSERTION` 为特定合作站点按 §5 规范签发的断言）。所有"样例结果"为真实环境实测输出。

---

# 1. 概述

## 1.1 背景

平台采用**定向合作模式**，仅对指定的特定第三方站点开放静默授权，不对外提供任何自助注册接口。通过在平台服务端手动配置合作方信息，实现该站点**全部已登录用户**免注册、免登录页地获得本平台作者身份与 token：

- 平台管理员在后端配置文件或数据库中手动预置合作站点的 `app_id`、共享密钥 `app_secret`（或公钥）及 IP 白名单；
- 第三方站点后端为当前登录用户签发一个 **2~5 分钟有效**的签名断言（JWT）；
- 本平台后端验签后按 `(provider, external_user_id)` 查找或自动创建本地用户；
- 返回与 `login` 结构一致的本平台 JWT（24h 有效），后续直接以 `Authorization: Bearer {token}` 调用现有接口。

## 1.2 角色映射约定

- 第三方站点的**所有用户一律映射为平台 `author`（作者）角色**，不授予管理员权限；
- 断言中的用户组（`groups`）仅作为信息透传展示，不参与角色映射；
- 如需个别用户升级为管理员，由平台管理员在后台数据库直接指定。

## 1.3 整体流程

```
第三方用户已在指定站点登录

指定站点后端                             本平台后端（已预置该站点配置）
   |-- 1. 生成短时断言 JWT（2~5 分钟有效）->|
   |-- 2. POST /api/v1/auth/external/exchange --|
   |                                       | 3. 校验 iss（必须在配置白名单中）、验签、校验 aud/exp/jti
   |                                       | 4. 按 (iss, sub) 查找或自动创建本地 author 用户
   |<-- 5. 200 { token, user_info } -------|
   |-- 6. 将 token 交给指定站点前端 -------->|

之后用户持 Authorization: Bearer {token} 直接调用本平台写稿与卡片管理 API
```

---

# 2. 合作站点手动配置规范（服务端白名单）

为了保证安全性并杜绝任意第三方接入，平台**不提供任何外部注册 API**。所有受信任的第三方站点均由平台管理员在后端配置文件（如 `config.json` / `.env`）或系统数据库中**手动预置录入**。

## 2.1 配置项定义

| 配置字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| app_id | string | ✅ | 合作站点唯一标识，如 `site_forum`，对应断言中的 `iss` |
| name | string | ✅ | 合作站点显示名称，如“主站论坛” |
| app_secret | string | ✅ | 双方约定的共享密钥（HS256 算法），线下安全交付 |
| algorithm | string | ✅ | 签名算法，默认推荐 `HS256`（亦支持 `RS256` / `ES256`） |
| public_key | string | ❌ | 使用 RS256/ES256 时必填，合作站点的公钥（PEM 格式） |
| enabled | bool | ✅ | 是否启用该站点接入。设为 `false` 可一键熔断拦截 |
| ip_allowlist | string[] | ❌ | 允许发起 exchange 的第三方服务器出口 IP 列表；空数组 `[]` 表示不限制 IP |

## 2.2 服务端配置示例（JSON 格式）

```json
{
  "external_apps": {
    "site_forum": {
      "name": "主站论坛",
      "algorithm": "HS256",
      "app_secret": "J8k2mX9pQ4wR7tY3vB6nC1zF5hA0sD2g",
      "enabled": true,
      "ip_allowlist": [
        "192.168.1.100",
        "203.0.113.10"
      ]
    }
  }
}
```

> **接入流程说明**：
> 1. 平台管理员生成高强度随机密钥 `app_secret`，连同分配的 `app_id` 线下安全告知第三方后端开发者；
> 2. 平台管理员将上述配置写入后端配置文件或数据库；
> 3. 未在配置中列出、或 `enabled: false` 的应用发起的任何换票请求均会被直接拦截并返回 `403`。

---

# 3. 认证模块

## 3.1 静默授权换取 token（核心接口）

| 项 | 值 |
|---|---|
| 方法 / 路径 | `POST /api/v1/auth/external/exchange` |
| 认证 | 无需登录，**用断言本身认证** |
| 限流 | 按 app 1000 次/小时 + 按外部用户 10 次/分钟（429） |
| 说明 | 校验第三方签名断言，映射/自动创建本地用户，签发平台 JWT（24h，与 `login` 一致） |

**请求体**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| assertion | string | ✅ | 第三方签发的 JWT 断言，规范见 §5 |

**测试用例**

```bash
curl -X POST http://localhost:8080/api/v1/auth/external/exchange \
  -H 'Content-Type: application/json' \
  -d "{\"assertion\":\"$ASSERTION\"}"
```

**样例结果（200）**

```json
{
  "code": 200,
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJleHAiOjE3ODU3NTczMjMsInVzZXJfaWQiOjl9.xxxx",
    "user_info": {
      "user_id": 9,
      "username": "site_forum_forum_user_1234",
      "nickname": "小明",
      "role": "author",
      "provider": "site_forum",
      "external_user_id": "forum_user_1234",
      "groups": ["member"]
    }
  },
  "message": "静默授权成功"
}
```

**响应字段（data）**

| 字段 | 类型 | 说明 |
|---|---|---|
| token | string | 平台 JWT，有效期 24h，调用现有接口时放 `Authorization: Bearer {token}` |
| user_info | object | 用户信息，见下表 |

**user_info 字段**

| 字段 | 类型 | 说明 |
|---|---|---|
| user_id | int | 平台用户 ID（首次自动建号后固定不变） |
| username | string | 平台登录名，规则为 `{provider}_{external_user_id}` |
| nickname | string | 展示昵称，取断言 `name`；未携带时为 `external_user_id` |
| role | string | **固定为 `author`**，不授予管理员 |
| provider | string | 第三方应用 ID（即断言 `iss`） |
| external_user_id | string | 第三方站点内用户唯一 ID（即断言 `sub`） |
| groups | string[] | 第三方用户组信息透传（断言 `groups`），可空数组 |

**样例结果（断言签名错误 / 过期 / aud 不匹配，401）**

```json
{ "code": 401, "message": "断言无效或已过期" }
```

**样例结果（jti 重放，401）**

```json
{ "code": 401, "message": "断言已使用过，请勿重放" }
```

**样例结果（应用未配置 / 应用未启用 / IP 不在白名单，403）**

```json
{ "code": 403, "message": "第三方应用未配置或未启用" }
```

```json
{ "code": 403, "message": "来源 IP 不在白名单内" }
```

**样例结果（限流，429）**

```json
{
  "code": 429,
  "data": { "action": "external_exchange", "identity": "user:site_forum:forum_user_1234", "limit": 10, "retry_after": 58 },
  "message": "操作过于频繁：external_exchange 次数已达上限（10 次/分钟），请稍后再试"
}
```

## 3.2 当前身份查询

| 项 | 值 |
|---|---|
| 方法 / 路径 | `GET /api/v1/auth/me` |
| 认证 | 必须 |
| 说明 | 返回当前 token 对应的用户信息；前端刷新登录态、第三方校验令牌都用它 |

**测试用例**

```bash
curl http://localhost:8080/api/v1/auth/me \
  -H "Authorization: Bearer $TOKEN"
```

**样例结果（200）**

```json
{
  "code": 200,
  "data": {
    "user_info": {
      "user_id": 9,
      "username": "site_forum_forum_user_1234",
      "nickname": "小明",
      "role": "author",
      "provider": "site_forum",
      "external_user_id": "forum_user_1234",
      "groups": ["member"]
    }
  },
  "message": "ok"
}
```

**样例结果（未认证，401）**

```json
{ "code": 401, "message": "未认证或 token 已过期" }
```

---

# 4. 自动建号与用户映射规则

1. **用户名**：固定规则 `{provider}_{external_user_id}`，例如 `site_forum_forum_user_1234`，天然全局唯一，避免与平台注册账号冲突。
2. **昵称**：取断言 `name`（`nickname` 同义字段也可），未携带时回退为 `external_user_id`。
3. **角色**：一律 `author`；断言中的 `group` / `groups` 仅透传展示，不参与角色映射，也不授予管理员。
4. **首次访问**：自动创建本地用户并绑定外部身份，无密码；**再次访问**返回同一 `user_id`，不会重复建号。
5. **绑定唯一性**：数据库对 `(provider, external_user_id)` 建唯一约束，同一外部用户只能对应一个平台用户。

---

# 5. 断言 JWT 规范（第三方签发）

## 5.1 Header

| 字段 | 必填 | 说明 |
|---|---|---|
| alg | ✅ | `HS256`（共享密钥）或 `RS256` / `ES256`（第三方私钥签名） |
| kid | 建议 | 密钥版本号，用于密钥版本区分 |

## 5.2 Payload

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| iss | string | ✅ | 第三方应用 ID，必须等于平台配置的 `app_id` |
| sub | string | ✅ | 第三方站点内用户唯一 ID，**对该用户永久不变** |
| aud | string | ✅ | 本平台 audience，等于 exchange 接口完整 URL（与后端配置一致） |
| iat | int64 | ✅ | Unix 秒，签发时间 |
| exp | int64 | ✅ | Unix 秒，**建议 2~5 分钟后过期** |
| jti | string | ✅ | 唯一断言 ID（建议 UUID），用于防重放 |
| name | string | ❌ | 展示昵称，自动建号时写入 |
| groups | string[] | ❌ | 第三方用户组，仅透传展示 |

## 5.3 示例 payload

```json
{
  "iss": "site_forum",
  "sub": "forum_user_1234",
  "aud": "http://localhost:8080/api/v1/auth/external/exchange",
  "iat": 1789999969,
  "exp": 1789999999,
  "jti": "11ec5d10-a2e3-4e2a-bd55-5dd01eb3f9d3",
  "name": "小明",
  "groups": ["member"]
}
```

对应完整断言（`header.payload.signature`，signature 由第三方使用线下分配的共享密钥生成）：

```
eyJhbGciOiJIUzI1NiIsImtpZCI6InYxIn0._mmI4iLCJncm91cHMiOlsibWVtYmVyIl19.SIGNATURE
```

## 5.4 第三方生成断言示例（Python）

```python
import time
import uuid
import jwt

payload = {
    "iss": "site_forum",                       # 平台分配的 app_id
    "sub": "forum_user_1234",                  # 该用户在第三方站点的唯一 ID
    "aud": "http://localhost:8080/api/v1/auth/external/exchange",
    "iat": int(time.time()),
    "exp": int(time.time()) + 300,             # 5 分钟有效
    "jti": str(uuid.uuid4()),
    "name": "小明",
    "groups": ["member"],
}

assertion = jwt.encode(
    payload,
    "双方线下约定的共享密钥",                     # HS256；RS256 时改为第三方私钥
    algorithm="HS256",
    headers={"kid": "v1"},
)
```

---

# 6. 频率限制汇总

| 动作 | 涉及接口 | 额度 | 计数主体 |
|---|---|---|---|
| external_exchange | `POST /auth/external/exchange` | 1000 次/小时 | 第三方应用（app） |
| external_exchange | `POST /auth/external/exchange` | 10 次/分钟 | 单个外部用户（provider + external_user_id） |
| read | `GET /auth/me` | 100 次/小时 | 访客 IP |

429 响应含 `data.retry_after`（冷却剩余秒数）。注意：限流计数发生在业务校验**之前**，被拒请求同样消耗额度。

---

# 7. 错误码汇总

| code | 场景 |
|---|---|
| 400 | 缺少 `assertion`、断言 JSON 解析失败 |
| 401 | 断言签名无效、过期、aud 不匹配、jti 重放；token 缺失或过期 |
| 403 | 应用未配置/未启用、IP 不在白名单 |
| 429 | 按 app 或按用户限流 |
| 500 | 服务端错误 |

---

# 8. 测试用例（回归建议）

以下为后端实现后的回归用例，均可用 curl 直接执行：

1. 合法断言 → 200，返回 `token` 与 `user_info`，首次调用自动建号且 `role` 为 `author`。
2. 同一断言重复提交 → 401（jti 重放）。
3. 篡改 payload 任意字段 → 401（签名不匹配）。
4. `exp` 已过期 → 401。
5. 未在配置白名单中的 `iss` / 已禁用应用（`enabled: false`） → 403。
6. 来源 IP 不在白名单（已配置 `ip_allowlist` 时）→ 403。
7. 同一外部用户第二次调用 → 200，`user_id` 与首次一致（不重复建号）。
8. 连续高频调用 → 429，且响应含 `retry_after`。
9. 拿返回 token 调 `GET /api/v1/auth/me` → 200，身份一致。
10. 拿返回 token 调创建卡片等需认证接口 → 按 `author` 角色正常放行；调管理接口 → 403。

---

# 9. 安全约定

| 项 | 要求 |
|---|---|
| 断言有效期 | 2~5 分钟，越短越好；平台签发的 token 仍为 24h |
| 防重放 | `jti` 唯一 + 缓存至断言过期；同一 jti 二次使用直接拒绝 |
| audience | `aud` 必须等于平台配置的 exchange 接口地址，防止断言被用于其他服务 |
| 算法白名单 | 只允许 `HS256 / RS256 / ES256`，拒绝 `none` 与弱算法 |
| 白名单管控 | 后端纯静态/数据库配置管理，不对外暴露任何注册接口 |
| IP 白名单 | 可选；开启后仅允许第三方服务器出口 IP 调用 exchange |
| 传输安全 | 全程 HTTPS；断言与 token 不落日志 |
| 审计 | 记录 provider、external_user_id、来源 IP、结果；失败不记敏感字段 |
| 密钥保管 | `app_secret` 由双方管理员线下交付，只存在于双方后端，**严禁下发到浏览器** |

---

# 10. 备注

- 本接口属于 `API_file_08031832.md` 认证模块的补充；换取到的 `token` 与 `login` 返回的 token 完全等价，可直接用于画廊、卡片、季度等全部现有接口。
- token 为无状态 JWT，**不提供即时撤销接口**，到期自动失效（24h）。
- 第三方站点所有用户固定 `author` 角色；管理员只能在平台数据库内手动指定。
- 若第三方站点未来需要区分不同用户组权限，可在应用配置中增加 `group_role_map`。
